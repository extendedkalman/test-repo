name: use it

on: [push]

jobs:
  build:
    runs-on: self-hosted
    steps:
    - name: checkout repo
      uses: actions/checkout@v2

    - name: action usage
      uses: extendedkalman/test-repo/test@v2
      with:
        message: "Merhaba, Dünya!"
