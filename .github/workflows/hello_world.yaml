name: hello world workflow

on:
  push:
    branches:
      - main

  workflow_dispatch:


jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: hello world
        run: echo "Hello world"
        shell: bash

  goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: goodbye world
        run: echo ${{ github.workspace }}
        shell: bash
  
  test:
    runs-on: windows
    steps:
      name: "test powershell"
      shell: powershell
      run: |
	Write-Output "Hello World 123"
